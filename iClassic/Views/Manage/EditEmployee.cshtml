@using iClassic.Helper;
@using iClassic.Models;
@model EmployeeEditModel
@{
    ViewBag.Class = "fa fa-user";
    ViewBag.Title = "Sửa nhân viên: " + Model.Name;
    ViewBag.Breadcrumbs = "<a href='" + Url.Action("Employees") + "'>Nhân viên</a>" +
                            "<span>Sửa nhân viên</span>";
}
<div class="row mb10 mt10">
    <div class="col-md-12">
        <h3 class="panel-title">
            @ViewBag.Title
        </h3>
    </div>
</div>
@using (Html.BeginForm(null, null, FormMethod.Post, new { role = "form" }))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    <div class="panel panel-default">
        <div class="panel-body">
            <div class="row mb10">
                <div class="col-md-6">
                    @if (User.IsInRole(RoleList.SupperAdmin))
                    {
                        <div class="form-group">
                            <label for="UserName">Chi nhánh<span class="asterisk">*</span></label>
                            @Html.DropDownList("BranchId", null, htmlAttributes: new { @class = "form-control" })
                            @Html.ValidationMessageFor(m => m.BranchId, "", new { @class = "text-danger" })
                        </div>
                    }
                    else
                    {
                        @Html.HiddenFor(model => model.BranchId)
                    }
                    <div class="form-group">
                        <label for="UserName">Tên tài khoản <span class="asterisk">*</span></label>
                        @Html.TextBoxFor(m => m.UserName, new { @class = "form-control", placeholder = "Tên tài khoản" })
                        @Html.ValidationMessageFor(m => m.UserName, "", new { @class = "text-danger" })
                    </div>
                    <div class="form-group">
                        <label for="Email">E-mail</label>
                        @Html.TextBoxFor(m => m.Email, new { @class = "form-control", placeholder = "E-mail" })
                        @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger" })
                    </div>
                    <div class="form-group">
                        <label for="Name">Họ tên</label>
                        @Html.TextBoxFor(m => m.Name, new { @class = "form-control", placeholder = "Họ tên" })
                        @Html.ValidationMessageFor(m => m.Name, "", new { @class = "text-danger" })
                    </div>
                    <div class="form-group">
                        <label for="Password">Quyền hạn<span class="asterisk">*</span></label>
                        <select class="form-control" name="Role">
                            <option value="@RoleList.Employee">Nhân viên chi nhánh</option>
                            @if (User.IsInRole(RoleList.SupperAdmin))
                            {
                                <option value="@RoleList.Admin" @(Model.Role == RoleList.Admin ? "selected" : "")>Quản lý chi nhánh @Model.Role</option>
                            }
                        </select>
                        @Html.ValidationMessageFor(m => m.Role, "", new { @class = "text-danger" })
                    </div>
                    <div class="form-group">
                        <label for="IsActive">Trạng thái</label>
                        <select name="IsActive" class="form-control">
                            <option value="true" @(Model.IsActive ? "selected" : "")>Hoạt động</option>
                            <option value="false" @(!Model.IsActive ? "selected" : "")>Dừng hoạt động</option>
                        </select>
                    </div>
                </div>
            </div>
        </div><!-- panel-body -->
    </div><!-- panel -->
    <div class="row">
        <div class="col-md-12">
            <button type="submit" class="btn btn-lightblue">Save</button>
            <a href="@Url.Action("Employees")" class="btn btn-default">Quay lại</a>
        </div>
    </div>
}

@section scripts{
    @Scripts.Render("~/bundles/jqueryval")
}